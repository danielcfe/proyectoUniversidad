$(document).ready(function() {
	// Stuff to do as soon as the DOM is ready;
/*	$('.hero-unit').on('click','#newadm',function(e){
		e.preventDefault();
		alert('entra');
		var data = $('#formUsuarios').serialize();
		console.dir(data);
	});
*/

});
